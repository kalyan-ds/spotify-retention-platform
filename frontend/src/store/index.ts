// Placeholder for global store configuration.
// Future implementation could use Zustand, Redux, etc.

export interface RootState {
  version: string;
}

export const rootStore: RootState = {
  version: '1.0.0',
};
