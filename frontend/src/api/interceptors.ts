// Placeholder for API interceptors (e.g. adding auth tokens, handling 401s).
export const setupInterceptors = (client: any) => {
  // implementation will attach handlers to the client
  return client;
};
