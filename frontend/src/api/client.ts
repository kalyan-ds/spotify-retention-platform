import { ENV } from '@/config/env';
// Placeholder for API client setup (e.g. Axios instance or Fetch wrapper).

export const apiClient = {
  baseURL: ENV.API_BASE_URL,
  // get, post, put, delete methods to be implemented
};
